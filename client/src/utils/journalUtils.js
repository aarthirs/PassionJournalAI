
export const calculateCurrentStreak = (history) => {
  if (!history.length) return 0;

  // Temporary implementation
  // Later we'll compare actual dates from the backend.
  return history.length;
};